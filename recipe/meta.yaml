{% set name = "ipyaladin" %}
{% set version = "0.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/ipyaladin-{{ version }}.tar.gz
  sha256: f8719c8439599a19402dd4081ccb8214e20c3b2ca062a2802b3055217976daa7

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation


requirements:
  host:
    - python >=3.8
    - hatchling
    - hatch-jupyter-builder >=0.5.0
    - pip
  run:
    - python >=3.8
    - anywidget
    - astropy
    - astroquery

test:
  imports:
    - ipyaladin
  commands:
    - pip check
  requires:
    - pip

about:
  license: BSD-3-Clause
  license_file: LICENSE.md
  home: https://github.com/cds-astro/ipyaladin
  summary: ipyaladin
  description: ipyaladin, a Jupyter widget for Aladin Lite, the sky viewer
  doc_url: https://cds-astro.github.io/ipyaladin/
  dev_url: https://github.com/cds-astro/ipyaladin

extra:
  recipe-maintainers:
    - bmatthieu3
    - tboch
